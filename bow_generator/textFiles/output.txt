We should add a caveat asking if you really want to do this, as if you do it N times on a dictionary with E elements you'll leak(/use) O(N*E) memory with all the deep copies. If you merely want a read-only (shallow copy), do d.pop(key). But if anything ever modifies the shallow copy, you have a well-known problem with aliasing. It helps if you tell us the wider context. (Is anything else ever modifying the di
